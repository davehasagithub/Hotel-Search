<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_sort"
        android:icon="@drawable/ic_sort_24dp"
        android:orderInCategory="1"
        app:showAsAction="ifRoom"
        android:title="@string/menu_sort">

        <menu>

            <group
                android:orderInCategory="100"
                android:checkableBehavior="single">

                <item
                    android:id="@+id/action_sort_by_hotel_name"
                    android:orderInCategory="1"
                    android:title="@string/sort_by_hotel_name"
                    android:checked="false"
                    app:showAsAction="never" />

                <item
                    android:id="@+id/action_sort_by_price"
                    android:orderInCategory="2"
                    android:title="@string/sort_by_price"
                    android:checked="false"
                    app:showAsAction="never" />

                <item
                    android:id="@+id/action_sort_by_guest_rating"
                    android:orderInCategory="3"
                    android:title="@string/sort_by_guest_rating"
                    android:checked="false"
                    app:showAsAction="never" />

            </group>
        </menu>
    </item>

</menu>